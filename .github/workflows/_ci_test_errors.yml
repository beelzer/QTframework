name: Test Workflow With Errors

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "test"
      # Actionlint error: missing shell
      - run: |
          if [ -f file ]; then
            echo "exists"
          fi
